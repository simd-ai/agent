"""Compressible solver family.

Solvers that carry a compressible energy equation (no buoyancy, no
multi-region).  Mirrors the OpenFOAM ``tutorials/compressible`` directory.

Contents:
  * ``rhoSimpleFoam``  — steady compressible RANS with energy
  * ``rhoPimpleFoam``  — transient compressible RANS with energy
"""
