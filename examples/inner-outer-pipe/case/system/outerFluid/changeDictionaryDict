FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system/outerFluid";
    object      changeDictionaryDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

T
{
    internalField   uniform 290;

    boundaryField
    {
    outerFluid_to_wall
    {
        type            compressible::turbulentTemperatureCoupledBaffleMixed;
        Tnbr            T;
        kappaMethod     fluidThermo;
        value           uniform 290;
    }
    }
}

// ************************************************************************* //
